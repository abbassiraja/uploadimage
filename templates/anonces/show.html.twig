{% extends 'base.html.twig' %}


{% block title %}Anonces{% endblock %}

{% block body %}
    <h1>Anonces</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anonce.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ anonce.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ anonce.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('anonces_index') }}">back to list</a>

    <a href="{{ path('anonces_edit', {'id': anonce.id}) }}">edit</a>

    {{ include('anonces/_delete_form.html.twig') }}
{% endblock %}
