{% extends 'base.html.twig' %}

{% block title %}Anonces index{% endblock %}

{% block body %}
    <h1>Anonces index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Content</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for anonce in anonces %}
            <tr>
                <td>{{ anonce.id }}</td>
                <td>{{ anonce.title }}</td>
                <td>{{ anonce.content }}</td>
                <td>
                    <a href="{{ path('anonces_show', {'id': anonce.id}) }}">show</a>
                    <a href="{{ path('anonces_edit', {'id': anonce.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('anonces_new') }}">Create new</a>
{% endblock %}
